//random(0,10);

//int x;

//color red = #F5B5B5;
//color orange = #F4CD7A;
//color yellow = #F4F3A1;
//color green = #ACF39D;
//color blue = #ABEDF4;
//color purple = #E3AAD6;


//ArrayList<Colour> colors = new ArrayList<Colour>();
//ArrayList<String> words = new ArrayList<String>();
//words.add("RED");
//words.add("ORANGE");
//words.add("YELLOW");

// etc etc

//words.get(int(random(0,2)));



//arrays


// declare and instantiate
// assigning values
// accessing values
// checking the size

// lets say we want to make a 

// String[] (string array)

// () parentheses
// [] brackets
// {} braces

// declaring and instantiating 
// String[]words (declaration, you can choose
// to add a semi colon and instantiate it later)
// String[]words = new String[x]; 
// where x = the number of things in the array

// you can do a short cut for declaring and instantiating

// String[]words = {"x","y","z","a","b","c"}; // etcetc 
// that loads the variables and such in one line of code instead of 
// "words.add" "words.add" "w

// same rule of the list starting at 0
// for the general rule of thumb for an index it's n-1

// if you wanna change the order of the things

// words[1] = "y";
// words[2] = "x"; 

// so after the code runs it will switch the order gang gang


// accessing values (is x "x"?)

// if (words[1] = "green") {
  //print("hello");
// }


// but can't do this exactly because arrays say no equal signs allowed >:(

// to find the size of an array

// [arrayName].length

// ie words[words.length-1]
